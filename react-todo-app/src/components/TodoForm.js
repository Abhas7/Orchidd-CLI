import React, { useState } from 'react';/** * A form component for adding new todo items. * @param {object} props - The component props. * @param {function} props.addTodo - Function to call when a new todo is added. */function TodoForm({ addTodo }) {  const [inputValue, setInputValue] = useState('');  /**   * Handles the form submission event.   * Prevents default form behavior, adds the todo, and clears the input.   * @param {Event} e - The form submission event.   */  const handleSubmit = (e) => {    e.preventDefault();    addTodo(inputValue); // addTodo function handles validation    setInputValue('');  };  return (    <form onSubmit={handleSubmit} className="todo-form">      <input        type="text"        value={inputValue}        onChange={(e) => setInputValue(e.target.value)}        placeholder="Add a new todo..."        aria-label="New todo text"      />      <button type="submit">Add Todo</button>    </form>  );};export default TodoForm;