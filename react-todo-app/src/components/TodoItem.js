import React from 'react';import './TodoItem.css';function TodoItem({ todo, toggleComplete, deleteTodo }) {  const handleToggle = () => {    toggleComplete(todo.id);  };  const handleDelete = () => {    // Add confirmation for delete for production-readiness    if (window.confirm(`Are you sure you want to delete "${todo.text}"?`)) {      deleteTodo(todo.id);    }  };  return (    <li className={`todo-item ${todo.completed ? 'completed' : ''}`}>      <input        type="checkbox"        checked={todo.completed}        onChange={handleToggle}        className="todo-checkbox"        id={`todo-${todo.id}`}        aria-label={`Mark "${todo.text}" as ${todo.completed ? 'not complete' : 'complete'}`}      />      <label htmlFor={`todo-${todo.id}`} className="todo-text">        {todo.text}      </label>      <button onClick={handleDelete} className="delete-button" aria-label={`Delete "${todo.text}"`}>        &#x2715; {/* Unicode 'X' mark */}      </button>    </li>  );};export default TodoItem;